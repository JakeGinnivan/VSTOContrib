<UserControl x:Class="FacebookToOutlook.Features.FacebookEvents.FacebookEventPaneView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="150">
    <StackPanel>
        <TextBlock FontWeight="Bold" FontSize="13" Text="Your RSVP" Margin="5" />
        <RadioButton Content="Attending" Margin="3" IsChecked="{Binding Attending, Mode=OneTime}" IsEnabled="{Binding Attending, Mode=OneTime}" />
        <RadioButton Content="Maybe Attending" Margin="3" IsChecked="{Binding MaybeAttending, Mode=OneTime}" IsEnabled="{Binding MaybeAttending, Mode=OneTime}" />
        <RadioButton Content="Not Attending" Margin="3" IsChecked="{Binding NotAttending, Mode=OneTime}" IsEnabled="{Binding NotAttending, Mode=OneTime}" />
        <TextBlock Margin="5"><Hyperlink Command="{Binding ViewEventCommand}">View Event</Hyperlink></TextBlock>
    </StackPanel>
</UserControl>
